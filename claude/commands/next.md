mainブランチに切り替えて最新を取得し、プランモードで次の作業を決めてください。

# 手順

1. `git checkout main` でメインブランチに切り替え
2. `git pull origin main` で最新を取得
3. EnterPlanMode ツールを呼び出してプランモードに切り替え
4. 「次は何に取り組みますか?」とユーザーに質問
5. 作業が完了して最初のPRができたら、Skill ツールを使って `/done` コマンドを呼び出してください
